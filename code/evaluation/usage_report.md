# Token usage and cost report

Generated by `code/evaluation/generate_usage_report.py` from `code/.cache/usage_log.jsonl`, 
which `code/evidence.py` appends to on every live Anthropic API call made while producing 
`output.csv`. Cached calls (repeat runs) are not re-logged, so this reflects the calls that 
actually happened, not calls that were served from the on-disk cache.

**Zero live Anthropic API calls were made for the run that produced the current `output.csv`.** The affordability engine (loader.py, events.py, forecast.py, decision.py) is fully deterministic and requires zero model calls on its own.

`evidence.py` reads messages.csv and images.csv per user and caches one structured JSON patch per user to `code/.cache/evidence/<user_id>.json`; if that cache file already exists, `build_overrides()` reads it directly and never calls any API, regardless of whether `ANTHROPIC_API_KEY` is set. **For this submission, all 219 cache files (covering all 200 requested users with message/image evidence) were populated by Claude Code -- the AI coding assistant used throughout development, reading the same messages/images `evidence.py`'s prompt would have sent, and applying the same untrusted-evidence and settled-cash rules -- rather than by a metered `anthropic.Anthropic().messages.create()` call.** This was a deliberate choice: a friend-supplied API key ($3 credit limit) was offered as a fallback but was not spent, since the agent-based extraction produces the same reasoning quality at zero programmatic cost. Because no API call was billed for this, there are no real token/cost figures to report for the evidence stage -- reporting fabricated numbers would misrepresent the run, so the table below is honestly $0.00 / 0 calls.

If `ANTHROPIC_API_KEY` is set and the cache is cleared, `evidence.py` will instead make one live `claude-sonnet-4-5-20250929` call per user with evidence (function unchanged, still wired in and tested) -- this script will then report real token/cost figures from `code/.cache/usage_log.jsonl` for that run.

| Model | Calls | Input tokens | Output tokens | Cache read | Cache write | Est. cost (USD) |
|---|---:|---:|---:|---:|---:|---:|
| — (Claude Code agent extraction, not a metered API) | 0 | 0 | 0 | 0 | 0 | $0.00 |

Total requests scored: see `dataset/requests.csv` (250 rows). Average tokens/request: 0.
